apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: dashboard-db
  namespace: dashboard
spec:
  instances: 1

  bootstrap:
    initdb:
      database: dashboard
      owner: admin

  storage:
    storageClass: cnpg
    size: 1Gi
    resizeInUseVolumes: true
  monitoring:
    enablePodMonitor: true

  managed:
    roles:
      - name: admin
        ensure: present
        login: true
        superuser: true
        passwordSecret:
          name: dashboard-db-admin
      - name: user
        ensure: present
        login: true
        passwordSecret:
          name: dashboard-db-user
