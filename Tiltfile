# ==================================================================
# Dashboard Development Environment
# ==================================================================

local_resource(
    "dev",
    cmd = ["/home/linuxbrew/.linuxbrew/bin/nub", "run", "dev", "--port", "0"],
    dir = "src/nuxt",
    labels = ["Frontend"],
    auto_init = False,
)
