# ==================================================================
# Dashboard Development Environment
# ==================================================================

local_resource(
    "dev",
    serve_cmd = "cd . && nub run dev --port 0",
    serve_dir = "src/nuxt",
    labels = ["Frontend"],
    auto_init = False,
)
