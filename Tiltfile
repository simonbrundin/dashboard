# ==================================================================
# Dashboard Development Environment
# ==================================================================

local_resource(
    "dev",
    cmd = "nub",
    args = ["run", "dev", "--port", "0"],
    dir = "src/nuxt",
    labels = ["Frontend"],
    auto_init = False,
)
